{
  "name": "alert-service",
  "version": "1.0.0",
  "description": "Alert Service (SOC/SIEM): consume alertas desde RabbitMQ (exchange alerts_events), las persiste en PostgreSQL (alerts_db) y expone una API REST de consulta y ciclo de vida (nueva/reconocida/cerrada).",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "amqplib": "^0.10.4",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
